# 数据与计算
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0

# 真实数据：默认走 src/data/eastmoney.py 直连东财，仅需 requests（最稳）
requests>=2.28.0

# 可选数据接口（akshare 依赖 jsonpath，部分环境编译失败；非必需）
# akshare>=1.12.0

# 统计与回归（行业/市值中性化、IC 显著性检验、过拟合诊断 DSR/PBO）
statsmodels>=0.13.0

# 进阶因子合成（梯度提升）。缺失时自动回退到岭回归线性合成，不影响运行。
scikit-learn>=1.2.0
lightgbm>=4.0.0

# 可视化（绩效曲线、分组回测、IC 衰减图）
matplotlib>=3.6.0

# 进度条（批量因子计算 / 滚动优化）
tqdm>=4.64.0
