# 数据与计算
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.9.0

# A股数据接口（演示用，可替换为 Tushare / Wind / 聚宽 等）
akshare>=1.12.0

# 统计与回归（行业/市值中性化、IC 显著性检验）
statsmodels>=0.13.0

# 可视化（绩效曲线、分组回测、IC 衰减图）
matplotlib>=3.6.0

# 进度条（批量因子计算 / 滚动优化）
tqdm>=4.64.0
